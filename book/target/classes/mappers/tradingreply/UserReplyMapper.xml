<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.book.user.reposiroty.IUserMapper">

	<!-- 회원 등록기능 -->
	<insert id="register">
	INSERT INTO user
    	(user_id, user_name, user_password, user_phone, user_email, user_auth)
		VALUES (#{user_id}, #{user_name}, #{user_password}, #{user_phone}, #{user_email}, #{user_auth}) 
	</insert>
	
	<!-- 아이디 || 이메일 중복체크 -->
	<select id="isDuplicate" parameterType="hashmap" resultType="int">
		SELECT COUNT(*) 
		FROM user
		<if test="kind == 'user_id'"> 
			WHERE user_id=#{info}
		</if> 
		<if test="kind == 'user_email'"> 
			WHERE user_email=#{info}
		</if> 
		<if test="kind == 'user_phone'">
			WHERE user_phone=#{info}
		</if>
	</select>

	<resultMap id="UserMap" type="com.spring.book.user.domain.userData">
		<id property="userId" column="user_id" />
		<result property="userPassword" column="user_password" />
		<result property="userName" column="user_name" />
		<result property="userPhone" column="user_phone" />
		<result property="userEmail" column="user_email" />
		<result property="userAuth" column="user_auth" />
	</resultMap>

</mapper>